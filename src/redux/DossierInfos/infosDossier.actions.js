export const addInfosDossier = (item) => ({
  type: "ADD_DOSSIER",
  payload: item,
});

export const addFactures = (item) => ({
  type: "ADD_FACTURES",
  payload: item,
});

export const setPopulation = (item) => ({
  type: "SET_POP",
  payload: item,
});
